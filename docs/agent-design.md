# AI Agent 设计

本文档描述 Nexus OS 中 AI Agent 模块的设计思路与架构规划。

> **当前状态**：`/agent` 页面 UI mock 已完成（对话消息流、任务卡片、语音/图片输入、左侧会话栏，与知识页侧边栏同构）；模型接入、意图理解、工具调用等真实能力尚未实现，将在 v0.4.0 阶段展开。本文档描述的是目标架构设计。

---

## 设计目标

AI Agent 不是聊天机器人，而是能够**理解需求、调用工具、执行复杂任务**的个人智能助手。

### 核心能力

1. **意图理解**：将用户的自然语言输入解析为结构化的操作意图
2. **任务规划**：将复杂任务拆解为可执行的步骤序列
3. **工具调用**：自动选择合适的系统工具并执行
4. **知识库优先问答（RAG）**：提问时先检索知识库，命中即基于库内内容回答，不够再联网或调用工具——这是 Agent 与知识库模块的核心衔接点
5. **结果整合**：汇总多步骤执行结果，生成用户可理解的回复
6. **上下文管理**：维护对话历史与任务状态，支持多轮交互

### 交互示例

```
用户：帮我整理昨天下载的资料，并生成总结。

Agent 内部流程：
  1. 扫描下载目录（调用文件管理模块）
  2. 按文件类型/日期分类
  3. 提取文件内容（OCR / 文本解析）
  4. 生成摘要总结（调用 LLM）
  5. 保存到知识库

Agent 回复：
  "已处理 8 个文件：3 份 PDF、2 张图片、3 个文档。
   已生成总结并保存到知识库「昨日下载」分类下。"
```

---

## 架构规划

```
用户输入（自然语言）
    │
    ▼
┌──────────────────┐
│  意图理解引擎     │  解析用户输入 → 结构化意图
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  任务规划器       │  将意图拆解为执行计划（步骤序列）
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  工具调用协调器   │  选择工具 → 传参 → 执行 → 收集结果
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  回复生成器       │  汇总结果 → 自然语言回复
└──────────────────┘
```

### 子模块说明

| 子模块 | 职责 | 关键技术 |
|--------|------|----------|
| 意图理解引擎 | 自然语言 → 结构化意图 | LLM Function Calling / NLU |
| 任务规划器 | 意图 → 多步骤执行计划 | LLM 推理 + 模板匹配 |
| 工具调用协调器 | 选择/执行工具，管理调用链 | Tool Registry + 参数校验 |
| 回复生成器 | 结果汇总 → 自然语言输出 | LLM 文本生成 |
| 记忆管理 | 对话历史 + 任务状态持久化 | 短期/长期记忆存储 |

---

## 待决设计问题

以下问题将在 v0.4.0 开发阶段确定：

- **模型选型**：使用哪个 LLM 作为核心推理引擎（本地 / 云端 API）
- **工具注册机制**：如何让 Agent 感知并调用工具中心的工具
- **对话界面**：流式输出的技术方案、Markdown 渲染、代码高亮
- **上下文策略**：上下文窗口管理、历史消息压缩、关键信息提取
- **安全边界**：Agent 操作权限控制，避免误操作（如误删文件）
- **多模型支持**：是否支持用户切换不同的 AI 模型

---

## 关联文档

- 系统架构：[architecture.md](./architecture.md)
- 接口设计：[interfaces.md](./interfaces.md)
- 开发路线：[roadmap.md](./roadmap.md)（v0.4.0 阶段）
