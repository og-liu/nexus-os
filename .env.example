# Nexus OS 环境变量模板
# 使用方法：复制本文件为 .env.local，填入你的真实值后保存。
# 注意：.env.local 会被 .gitignore 忽略，绝不会提交到 Git，放心存放真实密钥。

# DeepSeek API 密钥（必填）
# 到 https://platform.deepseek.com/api_keys 创建后粘贴到等号右侧
DEEPSEEK_API_KEY=

# 使用的模型（可选，不填则使用默认值 deepseek-v4-flash）
DEEPSEEK_MODEL=deepseek-v4-flash